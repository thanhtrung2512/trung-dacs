# Tên ứng dụng
spring.application.name=spbn3

# Cấu hình datasource MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/spbn3?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=25122005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Cấu hình JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update   # Tự động tạo/bổ sung bảng nếu chưa có
spring.jpa.show-sql=true               # Hiển thị SQL ra console
spring.jpa.properties.hibernate.format_sql=true  # Định dạng SQL đẹp
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
